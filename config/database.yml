default: &default
  adapter: postgresql
  pool: 5
  timeout: 5000

development:
  <<: *default
  database: provisioner_development

test:
  <<: *default
  database: provisioner_test

production:
  <<: *default
  url: <%= ENV['DATABASE_URL'] %>
